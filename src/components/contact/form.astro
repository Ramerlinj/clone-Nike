---
---

<section class="w-full px-4 md:px-12 py-16 bg-primary">
    <div class="max-w-3xl mx-auto">
        <div class="text-center mb-12">
            <h2 class="text-4xl font-nike-bold text-secondary mb-4">Envíanos un Mensaje</h2>
            <p class="text-lg text-white max-w-xl mx-auto">
                ¿Tienes alguna pregunta o quieres colaborar? Nos encantaría saber de ti.
            </p>
        </div>
        
        <div class="bg-gray-900 p-8 rounded-lg">
            <div class="grid grid-cols-1 md:grid-cols-2 gap-6 mb-6">
                <div>
                    <label for="firstName" class="block text-white font-nike-medium mb-2">
                        Nombre *
                    </label>
                    <input 
                        type="text" 
                        id="firstName" 
                        name="firstName"
                        required
                        class="w-full px-4 py-3 bg-gray-800 border border-gray-700 rounded-lg text-white focus:border-secondary focus:outline-none transition-colors"
                        placeholder="Ingresa tu nombre"
                    />
                </div>
                <div>
                    <label for="lastName" class="block text-white font-nike-medium mb-2">
                        Apellido *
                    </label>
                    <input 
                        type="text" 
                        id="lastName" 
                        name="lastName"
                        required
                        class="w-full px-4 py-3 bg-gray-800 border border-gray-700 rounded-lg text-white focus:border-secondary focus:outline-none transition-colors"
                        placeholder="Ingresa tu apellido"
                    />
                </div>
            </div>
            
            <div class="mb-6">
                <label for="email" class="block text-white font-nike-medium mb-2">
                    Correo Electrónico *
                </label>
                <input 
                    type="email" 
                    id="email" 
                    name="email"
                    required
                    class="w-full px-4 py-3 bg-gray-800 border border-gray-700 rounded-lg text-white focus:border-secondary focus:outline-none transition-colors"
                    placeholder="Ingresa tu correo electrónico"
                />
            </div>
            
            <div class="mb-6">
                <label for="subject" class="block text-white font-nike-medium mb-2">
                    Asunto
                </label>
                <select 
                    id="subject" 
                    name="subject"
                    class="w-full px-4 py-3 bg-gray-800 border border-gray-700 rounded-lg text-white focus:border-secondary focus:outline-none transition-colors"
                >
                    <option value="">Selecciona un tema</option>
                    <option value="general">Consulta General</option>
                    <option value="support">Atención al Cliente</option>
                    <option value="partnership">Asociación</option>
                    <option value="media">Relaciones con Medios</option>
                    <option value="careers">Empleos</option>
                </select>
            </div>
            
            <div class="mb-6">
                <label for="message" class="block text-white font-nike-medium mb-2">
                    Mensaje *
                </label>
                <textarea 
                    id="message" 
                    name="message"
                    rows="6"
                    required
                    class="w-full px-4 py-3 bg-gray-800 border border-gray-700 rounded-lg text-white focus:border-secondary focus:outline-none transition-colors resize-none"
                    placeholder="Cuéntanos más sobre tu consulta..."
                ></textarea>
            </div>
            
            <div class="flex flex-col sm:flex-row gap-4">
                <button 
                    onclick="alert('Formulario enviado! (simulado)')"
                    type="submit"
                    class="flex-1 cursor-pointer bg-secondary text-primary px-8 py-3 rounded-full font-nike-medium hover:bg-opacity-90 transition-colors"
                >
                    Enviar Mensaje
                </button>
                <button 
                    type="reset"
                    class="flex-1 cursor-pointer border border-gray-700 text-white px-8 py-3 rounded-full font-nike-medium hover:border-secondary hover:text-secondary transition-colors"
                >
                    Limpiar Formulario
                </button>
            </div>
        </div>
    </div>
</section>
